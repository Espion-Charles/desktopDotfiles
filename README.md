This is another project that is, for now, more personal.

If you want to install it for whatever reason, just do chmod +x ~/desktopDotfiles/install.sh, then run ~/desktopDotfiles/install.sh.

You need to be on a clean install, no checks if you already have some of the stuff (for now).
